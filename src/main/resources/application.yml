
spring:
  jpa:
    hibernate:
      ddl-auto: update

    properties:
      hibernate:
        format_sql: true #To beautify or pretty print the SQL
        show_sql: true

jwt:
  issuer: kitcd.share-delivery
  secret-key: test
  refresh-secret-key: test
  expiry-period: 60 # 분 단위
  refresh-expiry-period: 43200 # 분 단위


---
spring:
  config:
    activate:
      on-profile: hyunkim


  datasource:
    url: jdbc:h2:tcp://localhost/~/test;MODE=MySQL;DATABASE_TO_LOWER=TRUE;IGNORECASE=TRUE # mysql compatible h2
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: update

